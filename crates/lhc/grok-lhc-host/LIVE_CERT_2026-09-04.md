# Chunk 3B live certification — evidence, 2026-09-04

Binary: installed Grok-LHC **v0.3.0** (`grok 0.3.0 (8227f87cee07)`, the v0.3.0
candidate bytes at `~/.local/share/grok-lhc/current/bin/grok`, `~/.local/bin/grok`
and `~/.local/bin/grok-lhc` both resolve to it). LHC SDK pin `e9456a6e`, thread
schema 13, storage root `~/.grok-lhc` (product default, no override). Every
session below is a real session through the real binary — headless `grok -p`,
the TUI under tmux, or Lee's t3code server (port 3773, Grok instance,
grok-4.6). Model traffic is real (xAI API, Lee's account). Raw artifacts:
`/srv/work/grok-campaign/builders/heron/slice5/` (builder heron).

Native trigger for the CLI drills: `GROK_DEBUG_CONTEXT_WINDOW=150000`
(trigger 127.5k tokens, above the LHC continuation lower bound of 120k).
`ViewCompactParams` were never shrunk (runbook rule). t3code drills ran at
production defaults and used `/compact`.

| Row | Result | Session / evidence | Notes |
|---|---|---|---|
| L1 real-model G2 | **PASS** | `L1-g2.log`, 16 s | probes OK (PromptSmoothing production; ToolResultSummary capability-only, DERIV-12); seed 60,054 est tokens vs full budget 36,000; entries 9, bands=1, degraded=3 (smooth-from-excerpt, receipt records rung), gaps=0; five gates PASS; B8.3 compared=1 informational=0. FINDING classified as expected compaction variance / input coverage: real body 10 items vs fixture 9. No fixture regen. |
| L2 shell kill with LHC ahead | **PASS** | `767012c4`, `L2-watch.json`, `L2-inspect-*.json`, `L2-reopen.log` | watcher polled `view_boundary`/`thread_view` at 0.5 ms and SIGKILLed the resume process 0.128 s in, at the LHC install (v28→v41, boundary 11→40); native left as the 553,040 B pre-replace body (sha `d61afe278702`). Reopen: write-back re-entered once (v42, same boundary, checkpoints 1→2), native → 9 items / 53,798 B (sha `2892f2e979b7`), band-text events 1→2 with distinct texts (no double record), recall correct. |
| L3 `/btw` on compacted session | **PASS** | `3f97d2c3`, `tui-L3-btw.txt`, `L3-L6-oneshots-summary.txt` | TUI `/btw <question>` answered "chapter04" from the compacted body (native 3 items with `[context · smooth]`). Headless `-p "/btw"` is model-handled (no host recap path) and also answered from the compacted body. |
| L4 memory flush on compacted session | **PASS** (memory enabled explicitly) | `tui-L4-flush.txt`, `~/.grok/memory/cli-ws-093a64fc/sessions/2026-09-04-interval-3f97d2c3.md` | memory is off by product default (`/flush` writes nothing → BLOCKED as the runbook says); with `GROK_MEMORY=1` the flush reads `get_conversation()` (the compacted native body) and wrote the session log in 9.4 s, no panic, no re-ingest of pre-compact history. |
| L5 equivalence under real traffic | **FINDING → FIXED (slice 5b, v0.3.1)** | v0.3.0: `equiv-debug.log`, `L5-equiv-repro.txt`; fix: `slice5b/live-debug.log` | v0.3.0 logged an informational divergence on **every** substituted turn: the native `System` prompt (captured as `runtime_note`) was served again as a `[runtime note]` synthetic user item on top of the native system prefix, and the injected `<system-reminder>` (recorded in persistence order) was hoisted ahead of `<user_info>`. Lee's ruling: bug — the host owns its System prefix and the items it injects; the recorded copies are record-only. Fix in `align_runtime_notes_with_native` (serving + write-back, record unchanged); see the addendum below for the live re-check. |
| L6.1 unknown `/lhc` subcommand, repair confirm case | **PASS** (asymmetry documented) | `L3-L6-oneshots-summary.txt` | unknown subcommand → status fallthrough, no panic. `/lhc repair` shows the plan; `repair confirm <bad-id>` → "no displayed repair plan for this session; run /lhc repair first" (plan is per process); `repair CONFIRM …` (uppercase) falls through to status — confirm keyword is case-sensitive. |
| L6.2 status early-return asymmetry | **PASS** | `tui-L62-*.txt` | (a) capturing session: `Capture active: true`, engine LHC after a served turn; (b) after `/lhc off` while a second TUI session captured on its own thread: `Capture active: false`, `ModelCall compact: n/a (capture inactive)`, health note "Process enabled but no capture worker for this session"; (c) second session `Capture active: true` on its own thread. No false-active, no hidden capture. |
| L6.3 config refresh on `/new` | **PASS** | `tui-L63-*.txt` | `/new` in session g1 while session g2 was mid-turn (two reads): g1 became `01a06bfd-cb95…` with LHC on and health ok; g2 finished its turn with correct codes; no panic, no cross-session bleed. |
| L6.4 telemetry / Replace recoverability | **PASS** (spot checks) | `unified.jsonl` `trace.upload.decision` (trace_upload=true, source remote); `upload/trace.rs`, `upload/feedback_archive.rs` | trace uploads carry per-turn artifacts (`metadata.json`, `full_prompt.txt`, tool definitions, images, plugin state) and the feedback archive tars the native session dir only; no code path references `GROK_LHC_ROOT` or the thread SQLite (`grep` over `upload/`, `remote/sync.rs`, `instrumentation.rs`: none). After Replace the native session dir keeps `compaction_checkpoints/*.json` with the pre-compact bodies (7 for the mid-turn drill) and the LHC SQLite keeps the full event log — FORK.md rollback holds. |
| Drill: mid-turn compact | **PASS with limitation** | `3f97d2c3`, `drill1-midturn.log`, `drill1-recall.log`, `drill1-inspect-after-turn.json` | one 8-read turn crossed 127.5k mid-turn; hook 5 fired 7× inside the open turn (checkpoints 10:02:58–10:03:50 while t3 open at events 4→204). LHC compact point stayed at the turn start (turn parts not wired: `compact_point_upper_bound: None`), so native did not shrink until the turn closed (checkpoint bodies 17→108 items). One turn in the record (t3 closed completed), all 9 tool results present (tool_call events re-recorded per write-back: 61 for 9 ids). Restart (new process, `-r`): compact at first call → native 3 items, 37,880 input tokens, all 8 codes recalled. At a 60k window the LHC lower bound sits above the native trigger and the loop ran 13× (`drill1-*-60k*`). |
| Drill: images across compact + restart (CLI) | **PASS** | `767012c4`, `drill2-*.log` | read_file on a PNG → blob row image/png 520 B, correct description; 7-read turn; after the L2 kill + reopen the model still described the image and listed the codes. |
| Drill: images across compact + restart (t3code) | **PASS** | `t3/grok-drill-run.json`, `t3-drill-run3.log` | pasted PNG (attachment) → blob 768 B; read_file PNG → blob 520 B; three reads; `/compact` through the LHC choke → view v26, one smooth band (4,364 tokens) carrying the image placeholder, no base64 in events; server restart; recall with 0 tools named both images and the three codes. |
| Relay seat `grok-build-fork-steward` | **PASS** | `relay-oneshot-*.log`, agents.json backup `pre-heron-slice5-20260904T101906Z` | `relay.command` repointed to `~/.local/bin/grok`; validated and installed under the registry lock; lhc-console restarted; one-shot job `61c0c907` completed in ~75 s running ver 0.3.0 on thread `019fe65e`, which migrated 6 → 13 in place (6,154 events). |

## Adapter / harness at the same SHA

Tripwire ALL GREEN (sentinel 10/10, compile, fmt ×2, unit 177, golden 5,
certification 110, chunk3b 11, pin on `origin/main`).

## Not proven here

Lee's own session on the t3code Grok instance at production thresholds (256k
window, 217k trigger) with an auto-compact, restart and continuation is the
done-bar item that only Lee can run. Everything above was driven by the
builder.

## L5 addendum — slice 5b re-check on the fix (2026-09-04, local build `grok 0.3.1`)

Fresh headless session `6b47e033` in `slice5/cli-ws` on a local release build
of the fix (`slice5b/grok-0.3.1-local`, same source as commit `e41fd289`),
`GROK_DEBUG_CONTEXT_WINDOW=150000`, `GROK_LOG_FILE=slice5b/live-debug.log`
so the hook-4 warn line (first of each class per session, with the first
differing item) is captured.

| Turn | Served | Hook-4 result |
|---|---|---|
| 1 — "Reply with the single word OK" | substituted (LHC body) | **no divergence line of either class** (a length or shape mismatch is structural, so served == native in shape and, after projection, in content). On v0.3.0 the same first-turn shape logged structural + informational with `native_len=4 served_len=5`. |
| 2 — read `chapter01.txt` (new process, `-r`) | substituted (LHC body) | structural only: `native_len=7 served_len=7`, first diff index 4 = the opaque reasoning signature of turn 1 (`rs_…`, `encrypted_content`) not replayed in the resumed process; the visible reasoning text is identical. **Informational: none.** |

`/lhc status` in a third process shows fresh counters (they are process-wide),
so the evidence is the warn line, not the counters. Certification pins the
shape: `l5_runtime_notes_aligned_to_host_items_no_divergence` (served == native
item for item on the recorded first-turn shape, both counters 0, post-compact
shape, write-back fixpoint).

Not re-run on the fix: the t3code drill and the TUI rows (serving code path is
the same hook 4; the pinned test covers the post-compact shape).
